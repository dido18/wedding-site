{
  "name": "node-example",
  "version": "1.0.0",
  "description": "This example is so cool.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev":  "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "build": "npm install && tailwindcss -i ./src/input.css -o ./dist/output.css",
    "start": "serve"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.18"
  }
}
